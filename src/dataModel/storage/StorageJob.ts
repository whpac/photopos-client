/**
 * Represents a job that can be executen on a storage.
 * It may be deferred if the storage is unavailable.
 */
interface StorageJob {
    // TODO: Decide later what should be the interface of a storage job.
}

export default StorageJob;